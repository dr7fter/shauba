# 10 · 环境与命令

> 从 AGENTS.md v2.1 拆出（守 6KB 体积红线）。更新：2026-09-04

```bash
npm install          # 装依赖
npm run app          # 桌面端开发（热重载）
npm run dev          # 仅浏览器预览（mock 数据）
npm test             # 完整测试门禁（前端 60 + Rust 121）
cd src-tauri && cargo test --locked   # Rust 门禁（当前 121 个）
```

**提交前门禁：`cargo test --locked` 全绿 + 构建成功。**

⚠️ 本机 `npm run build` 会失败（环境问题，非项目问题）：vite 清空 `dist/` 会触发
safe-delete 守卫。**不要为此改 vite 配置**，改用：

```bash
npx tsc -b                              # 类型检查
npx vite build --emptyOutDir false      # 构建
```

⚠️ **改版本号后必须** `cd src-tauri && cargo update -p shuaba --offline`，
否则 `cargo test --locked` 失败。

## 脚本沙盒 `prototype/`（本地，已 gitignore）

**根目录只放被规范/流程直接引用的活工具（勿删）**：

- `gh-tunnel.cjs` —— 发版绕法：本地 CONNECT 隧道（`.agent/04` 引用，**勿删**）
- `make-payload.cjs` —— 抽取 Release body（`.agent/04` 引用，**勿删**）
- `upload-release.cjs` —— 上传 Release 资产
- `run-ssr.sh` + `ssr-*.tsx` —— 批改报告改版的五套 SSR 验证台（`.agent/plans/2026-09-04` 引用）

**其余按三桶归档（2026-09-12 整理，见 `prototype/README.md`）**：

- `_scripts/` —— 一次性脚本（dump / print / check / verify / inspect / analyze / probe 等）
- `_artifacts/` —— 脚本产出的 `*.txt` / `*.json` / `*.jsonl` 中间结果
- `_done/` —— 已弃用 / 调试残片 / 日志，**冻结观察，承诺 7 天后删除**

- **新的一次性脚本一律放 `prototype/_scripts/`，不进根目录**（AGENTS.md 红线 7）

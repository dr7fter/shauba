# 批改报告 · 内容呈现与细节优化 任务计划书

> 2026-09-04 起草 · 状态：**批次 A–D ✅ 已随 v1.9.1 发布（09-04 12:57）**；
> **批次 E（WP9–WP13）✅ 已完成（09-04 下午）——提示词契约重构 + 证据层上架 + 微观质感**。
> 遗留项见第八节。
> 来源：三轮讨论的合并——① 好老师批改建议评估 ② 数据模型勘察 ③ UI 细节体检 + 你补充的
> 「批改提示词可优化」「复制追问上下文要标准数一详解」两条新要求。
> 按仓库约定：方案确认后再动手；动手后每个 WP 独立 commit（中文 conventional）。

---

## 一、目标

把批改报告从「每题一段话」升级为「三秒看清今晚该干嘛」：

1. **扫读层有密度**：错题卡第一行直接看到病因、复做日期、药方类型
2. **错题进日程**：引擎已算好的 154 条复做排期露到批改现场
3. **抓住"假会"**：碰对/绕路的对题第一次可见、可统计
4. **追问得详解**：复制追问上下文贴给 AI，拿回的是考纲内标准详解，不是秒杀技巧

## 二、范围

**做**：批改报告（`src/components/report/`）、学习中心数据复用、回传格式、批改提示词、
全 App 字体与图标地基。

**不做**：

- ❌ **好友功能全部不动**（2026-09-04 你指示：暂停。`FriendsLadderView` /
  `FriendPublicReportModal` / `FriendShareCardModal` 等一律不碰）
- ❌ 报告框架结构（三栏、折叠段、动线六段——你满意的部分）
- ❌ ELO / HLTV rating 公式（个人化基准只做展示层；改公式走 `.agent/03` 另行评估）
- ❌ 题库源目录（红线，只读）
- ❌ 错题病因标签体系换血（aiming/concept/tactics 已与画像对齐，不动）
- ❌ 病灶行号「第 3→4 行」：草稿是图片、无行级文本，编行号违反不造数据（第三档结论）
- ❌ 母题身份 / 命题人三种改法：变式的载体是推题通道（noveltyPlan），不是报告文字（第三档结论）

## 三、已核实的现状（方案的事实基础）

| 事实 | 出处 |
|---|---|
| `review_tasks` 154 行，复做日期 100% 有值，stage/药方词表齐 | 09-04 库快照 |
| `normalized_error_class` 归一函数已把 计算/笔误→aiming，前端报告 0 引用 | `learning.rs:252` |
| `MistakeChain` 类型 + 后端查询已存在，学习中心在用，报告可复用 | `types.ts:895`、`lib.rs:6441` |
| 复制追问模板**实际代码与 02 分册描述不一致**：代码里没有放 `betterSolution`，且已揭示解法时会写「不要重复讲解法」——与你的详解需求直接冲突 | `ReportWindow.tsx:197-219` |
| 字体零打包：无 `@font-face`、无字体文件，实际渲染回退为雅黑+宋体 | 全 `src/` 核查 |
| 报告零 SVG 图标，✓/◐/✕/· 来自不同字体，基线不一 | `QuestionRail.tsx:12-17` |
| 折叠段已有完整按钮语义（role/tabIndex/键盘），复制按钮已有「已复制」反馈 | `ReportSection.tsx:30-36`、`ReportWindow.tsx:312` |

## 四、工作包（WP）

### 批次 A · 独立小活，最先落地（约 0.5 天）

**WP1 · 复制追问上下文 → 标准详解版**　优先级 P0

现状模板（`ReportWindow.tsx:197-219`）的问题是：揭示解法后反而告诉 AI「不要重复讲解法」，
且结尾只索要规则与动作——拿不到详解。改为：

```text
题号 #123
分类：高等数学 / 一元函数积分学 / …
题干：…

请给我这道题在考研数学一考纲范围内的标准详解：
1. 步骤完整，可直接写在答卷上；关键步骤注明依据（定理 / 公式名）
2. 只用考纲内方法：不用二级结论、秒杀技巧、特殊值凑答案
3. 详解完全基于题目独立推导，不要受下方我的过程影响

━━━ 详解写完再看这里：我当时的过程 ━━━
我的落笔入口：…
断点：…
为什么走不通：…
4. 对照你的详解，指出我从哪一步开始必须换路、当时该怎么识别；
   我的入口本身可行就沿它补完
```

要点：**题面与详解请求在前、我的断点过程隔在分割线后**——新 AI 先独立解完，
不被先前批改思路带偏，再对照指出换路点（2026-09-04 你补充的要求）。

- 改：`ReportWindow.tsx handleCopyContext` + 按钮反馈沿用现有「已复制」
- 同步修 `.agent/02-批改与诊断.md`「配套：复制给 AI 模板」一节——**文档与代码已脱节，以代码为准重写**
- 验收：贴给 AI 后返回完整标准详解，无秒杀技巧，末尾一条否定式规则

**WP2 · 批改提示词（02 分册）优化**　优先级 P0（纯文档）

- `batch.summary` 三段式纪律：对错分布 → errorCode 聚类（`E-027` 复发 2 次）→ 本次只带走一件事
- `methodSoundness` 写作纪律（配合 WP5）：correct 但特殊值/代选项/跳步 → `lucky`，
  summary 必须点破不可复现；correct 但 speed ≤ 60 → `detour`，betterSolution 给省时版
- `betterSolution` 定位澄清：保留「选填速算」角色；**标准详解不再由它承担，详解走追问通道**（WP1）
- 提醒：AI 若使用「符号抄错」标签，当前归一函数会落入 mixed 兜底类，需先补映射词（WP5 顺手）
- 验收：下一轮真实批改按新规产出，报告直接可读

### 批次 B · 视觉地基（约 0.5–1 天）

**WP3 · 字体打包**　优先级 P0（已拍板：三套全打）

- Inter var + JetBrains Mono + **霞鹜文楷常用子集**（约 2-4MB，刷题页题干 `--font-stem` 用）
  打 woff2，`@font-face` 进 `tokens.css`
- 报告只用 UI + Mono 两套；文楷服务刷题页题干

**WP4 · SVG 图标体系**　优先级 P0（挡 WP6 的路）

- 新建 `src/components/ui/Icon.tsx`，手绘 inline SVG（stroke `currentColor` / 1.75px），
  尺寸走已定义未使用的 `--icon-sm/md/lg` token
- 首批 9 个：chevron（替换 9px 字符三角）、✓、◐→半圆、✕、准星（瞄准失误）、
  书（概念盲区）、岔路（战术绕路）、回转箭头（复做）、骰子（碰对）
- 👑 保留 emoji（游戏化场景彩色 emoji 是对的）

### 批次 C · 数据露出（约 2 天，含测试）

**WP5 · methodSoundness 字段**　优先级 P1

- `lib.rs migrate_schema` 加列：`learning_evidence.method_soundness TEXT`（`sound|lucky|detour`，可空），
  沿用现有加列元组模式（幂等带默认值，安全）
- `.agent/01-回传格式.md`：`analysis` 与 `batchAttempts` 加字段；`.agent/02` 写作纪律见 WP2
- 顺手给 `normalize_error_class` 补「符号抄错 / 抄错」→ aiming 映射（含 Rust 测试）
- 前端：对题卡渲染「⚠ 碰对」「绕」chip；SessionPane 增「本场碰对 N / 绕路 N」独立计数行
  （已拍板：正确率与 ELO 口径不动，只让含水量可见）
- 验收：回传验证三连 + `cargo test --locked`；历史无数据题目留空（不编造）

**WP6 · 报告数据露出（第一档主体）**　优先级 P1

- 后端新增查询命令：按题号取 `errorClass / nextReviewAt / nextAction / severity / errorCode`
  （join `learning_evidence` / `review_tasks`，学习中心同款口径）
- 前端：
  - 错题卡 meta 行：病因 chip（**扩展色**：瞄准=cyan、概念=violet、战术=gold，不与严重度撞色）
    + 复做日期 + 药方类型（变式练/回看概念/快速重做/限时重做）
  - 报告顶部「聚类总诊断」三行：对错分布 → errorClass 众数类（含该类的 E 编号复发次数）
    → 本次一件事（该类中 severity 最高且最近的题）
  - 题目排序按价值：severity L1>L2>L3，聚类代表题优先（不再按题号）
- 回退文案：整场 errorClass 全是 uncertain 时，总诊断降级为普通摘要（不硬凑结论）
- 验收：用最近一场真实批改逐题核对显示；`npm test` + `cargo test --locked` 全绿

**WP7 · 参考用时个人化（展示层）**　优先级 P2

- 查询：attempts 按 `category_path` 取**做对题**中位用时，过滤 [5s, 1200s] 脏数据
- 展示：SessionPane 逐题行「用时 vs 你的基准」；样本 < 3 回退题型基准并标注
- **不改 rating/ELO 公式**

### 批次 D · 细节打磨（约 0.5 天，可与 C 并行）

**WP8 · UI 细节批次**　优先级 P2

- 阅读行宽：断点六段 13px×768px ≈ 55 字/行，超中文舒适区 → 阅读列收窄至 640-680px
  或 `rp-sec-bd` 升 `fs-md`（二选一，渲染后定）
- 等宽体硬编码统一为 `var(--font-mono)`；SessionPane 数字列加 `tabular-nums`
- `rp-sec-hd` 补 `:focus-visible` 焦点环（`var(--ring)`）
- 报告内按钮尺寸收进 `--h-sm/--h-md` token
- 断点段 `rp-kill` 收敛为左边线 + 浅底（已拍板）；报告整体素化——彩色底一律 `*-soft` 档，
  实色填充只留复发告警一个最强信号

## 五、执行顺序与依赖

```
批次 A（WP1 模板 + WP2 分册）     独立，当天可落地
批次 B（WP3 字体 → WP4 图标）     图标挡 WP6 的路
批次 C（WP5 字段 → WP6 露出 → WP7 基准）  WP5 的迁移先行，WP6 一次迁移窗口内顺带
批次 D（WP8 细节）                与 C 并行
```

估算合计 **3–4 个工作日**（含测试门禁）。每批次结束可独立交付、独立 commit。

## 六、门禁与风险

**每个 WP 提交前**：`npx tsc -b` + `npx vite build --emptyOutDir false` +
`cargo test --locked`（88）+ `npm test`（26）。

| 风险 | 对策 |
|---|---|
| 迁移破坏现有库 | 沿用 `migrate_schema` 加列元组模式（幂等带默认值）；不动 schema 直写（新纪律） |
| 聚类总诊断数据质量 | uncertain 占比高的场次给回退文案，不硬凑 |
| 字体体积影响更新包 | 文楷子集单独决策；Inter+Mono 仅 500KB |
| 改 UI 破坏学习中心 | MistakeChain 只复用查询口径，不改学习中心页面 |

## 七、决策点（2026-09-04 已全部拍板）

1. **文楷打包** ✅ 打。Inter var + JetBrains Mono + 霞鹜文楷常用子集一并进 woff2
2. **追问模板：纯详解** ✅ 去掉「末尾否定式规则」条；且新增要求——
   **新 AI 的思路不得被先前批改带偏**：题面与详解请求在前，我的断点过程放分割线之后，
   AI 先独立解完，再对照指出换路点
3. **断点段收敛 + 整体素化** ✅ `rp-kill` 收敛为左边线 + 浅底；彩色底一律 `*-soft` 档、
   素一点，实色填充只保留复发告警一个最强信号
4. **lucky 题处理** ✅ 采纳建议方案：逐题 chip 标注 + SessionPane 增「本场碰对 N / 绕路 N」
   独立计数行；**正确率与 ELO 口径不动**（作答结果是对的就是对的，只让含水量可见）

---

## 九、批次 E（2026-09-04 下午，WP9–WP13）

来源：第二轮体检（批改提示词结构问题 6 条 + 内容多余/缺失清单 + UI 微观三张表）。

### WP9 · 修断点动线从未真正入库的缺陷　优先级 P0（根因）

`scan_inbox`（`lib.rs:2291` 起）把回传文件反序列化成 `CodexPayload` 后
**重新序列化**存入 `codex_inbox.payload_json`（`insert_codex_payload`，`lib.rs:2476`）。
而 `CodexPayload` / `BatchAttempt` 都没声明 `diagnosis`——
所以 `diagnosis` 自 2026-09-03 接入报告（commit `ceb4423`）以来**从未真正入库**，
报告六段动线一直在走降级派生（`buildGradeFlow` 里 myEntry / whyDeadEnd / rule / acceptance
全是 `?? null`）。

- 修法：新增 `GradingDiagnosis` / `DiagnosisRule` / `DiagnosisFork` 强类型并挂到两个载荷结构上
- 回归：`batch_payload_keeps_diagnosis_and_new_fields_through_inbox`
  （走的是同一条 `insert_codex_payload` 路径，断言 `payload_json` 里字段仍在）
- 纪律沉淀：`.agent/01` 新增「两条工程约束」——新增回传字段必须同步结构体 + 留存测试

### WP10 · 提示词与量规重构

| # | 改动 | 位置 |
|---|---|---|
| 1 | 题面只给客观数字（`实际耗时 / 基准 N 秒`），删掉 App 替 AI 下的节奏评价词 | `lib.rs` 两处 `timing_info` |
| 2 | 基准耗时统一走 `services::rating::benchmark_seconds`，消掉三处硬编码；压力报告的 `bench = 600` 一并修正 | `lib.rs` |
| 3 | `errorTags` 主标签唯一化 + `secondaryTags`；归一函数改看**第一项** | `learning.rs::normalize_error_class` |
| 4 | 六维量规 / HLTV / 断点纪律外提到 `.agent/09-批改量规.md`，改量规不再重编译 | 新分册 + 两处提示词 |
| 5 | 固化状态归 App：提示词删「升级已固化」授权，报告端新增 `deriveConsolidation` | `.agent/09` 第一节 + `reportViewModel` |
| 6 | 契约新增 `stepScore` / `draftPaths` / `secondaryTags` / `whyItWorked`；废弃 `ratingTier` / `difficultyMultiplier`（难度系数改由 `question_difficulty_multiplier` 从题库派生）与维度内 `advice` | `lib.rs` + `.agent/01/02` |

### WP11 · 证据层上架（报告内容）

原题附图 · 我的草稿原件（`draftPaths`）· 我答 vs 标答 · 自评/实判/AI 置信 ·
有效步骤分 · 极差最大两维的 `evidence` 原句 · `techniqueLevel` + `independentDiscovery` ·
同 `errorCode` 复发时间线（新只读命令 `get_error_code_history`，不加表不加列）·
correct 题固化卡。压力报告的 grade 此前连 `diagnosis` 都不输出，已补齐与日常链路对齐。

### WP12 · 微观质感

字号阶梯（正文 13→14、明日动作 12→14、段标题、状态栏 10→11、注记 10→11）、
`.insight-summary strong` 等宽字体被 `font` 简写覆盖的缺陷、`font-synthesis-weight`、
`text-wrap: pretty`、阅读列 44rem / 宽视图 66rem、块级公式段距统一、
`--h-xs` 顶栏控件统一高度、自绘 checkbox、复制按钮文案固定 + toast、
`:active` 下沉全 App、`:focus-visible` 全站补齐、
图标 `strokeWidth` 光学归一、`partial` 与 `uncertain` 形状+底色双分离、
`👑`/`📌` 矢量化、空态补图标、条形行 `tabular-nums`。

### WP13 · 可信度可见化（补第一轮的 P0 尾部）

三处写库 `catch(() => {})` 改为可见（亲笔自省 / 明日计划 / 复制）；
历史查询失败时状态栏不再冒充「复发 0」，改显「历史不可用」；
`reportStatus === 'evidence-insufficient'` 上架；切题滚动复位；遮罩不再一点即关。

**验收**：`npx tsc -b` 干净、前端 60 / Rust 121 全绿、`vite build` 成功、
`bash prototype/run-ssr.sh` 五套 SSR（含新增 `ssr-evidence.tsx`）全绿。

## 十、遗留项（未做，附理由）

| 项 | 为什么没做 |
|---|---|
| `draftPaths` 落 `attempts` 列 | 报告从 `payload_json` 实时构建，已能显示；压力会话的 `report_json` 是确认时快照，**旧场次看不到新字段**，需重新批改才会补上。要历史可查再加列（`migrate_schema` 幂等模式） |
| 压力报告改为读时构建 | 与 codex 路径统一能一次性解决"历史快照看不到新字段"，但会动 `pressure_reports` 读取语义，单列一个 WP 评估 |
| 基准表跨语言一致性测试 | Rust `benchmark_seconds` 与前端 `utils.ts::benchmarkSeconds` 仍是两份实现，缺一条防漂移断言（本次已消掉 lib.rs 内三处重复） |
| 总诊断每切一题重复渲染 | 属呈现口味，建议收成可展开胶囊而非直接搬走 |
| 识别规则上提常驻 + 接 `FormulaDrawer` | 需要一次交互设计确认，不与本批混合 |
| 报告内键盘帮助（`KeyboardHelpModal`）、侧栏筛选 chip、"我不同意这次批改"复制模板 | 第一轮 P1/P2 尾巴，未纳入第二轮清单 |

## 十一、批次 E 交接清单（2026-09-04 15:40 移交）

代码改动已全部写完，**但最后一批改动之后没再跑门禁**（本机 `npm` / `npx` 命令被
Qoder 自动模式的安全分类器随机拒绝，`git` 类可通过）。接棒者按下面顺序收尾：

### 1. 复跑门禁（必做，未提交的原因就是这个）

```bash
npx tsc -b                              # 上次绿是在 CSS/图标/测试追加之前
npm test                                # 上次：前端 60 + Rust 121 全绿；此后又各加 1–2 条
npx vite build --emptyOutDir false      # 构建
bash prototype/run-ssr.sh               # 五套 SSR（新增 ssr-evidence.tsx 验证据层）
```

⚠️ 不要用 `... | tail` 判断成败——管道会把退出码变成 `tail` 的（本次就被骗过一次）。

上次绿之后新增、**尚未验证**的改动：
`rating.rs` 基准表测试、`report_view_model.test.js` 两条新测试、
`report.css`（答案行 `rp-yes/rp-no` 着色、证据段、时间线、焦点环、`math-inline`）、
`tokens.css`（`--h-xs`）、`ui.css`（`:active`/`:focus-visible`/`.insight-summary strong`）、
`DossierPane` / `SessionPane` 空态图标、`ReportWindow` 的 `evidence-insufficient` 上架、
`prototype/ssr-*.tsx` 四套验证台修补。

### 2. 提交（按文件名精确 stage）

工作树里有**不属于本批**的改动，别卷进来：
`characteristic.md`、`characteristic-core.md`、`.agent/07-取样与备考.md`、
`考研规划/取样策略-pro-v5.md`（+45/−10，疑似并行批改会话按第八节契约更新画像）、
未跟踪的 `scripts/_tmp_prob2.py` / `scripts/_tmp_prob_review.py`。
`prototype/` 与 554KB 的会话导出 md 也不进 git。

建议三个独立 commit：
① `fix(codex)`: diagnosis 与新增字段留存入库 + 提示词与量规重构（`lib.rs`、`learning.rs`、
`rating.rs`、`.agent/01·02·09`、`AGENTS.md`、计划书）
② `feat(report)`: 证据层上架（`types.ts`、`api.ts`、`reportViewModel.ts`、`report/*`、`tests/`）
③ `refactor(ui)`: 微观质感（`styles/*.css`、`ui/Icon.tsx`、`report/QuestionRail.tsx`）

### 3. 需要肉眼确认（无法用测试代替）

- `.rp-view .math-inline { line-height: 0 }` 是否造成行内公式基线位移；
- 新增四块（草稿原件 / 我答 vs 标答 / 复发时间线 / 评分证据 / correct 固化卡）
  在真实窗口里的行宽与层级；
- 顶栏控件统一 30px 后与分段器的视觉齐平。

### 4. 需要一次真实批改验证

新契约字段（`diagnosis` / `stepScore` / `draftPaths` / `secondaryTags` / correct 固化卡）
目前**只有回归测试的合成数据验证过**。下一次真实回传后按 `.agent/01` 验证三连核对：
草稿原件能不能显示、步骤分对不对、复发时间线有没有并排、correct 题有没有固化卡。
批改前先读 `.agent/09-批改量规.md`。

### 5. 评分侧风险（复核请读 `.agent/03`）

- `normalize_error_class` 改为只看**主标签第一项** → 病因聚类与明天推题会变（新回传起生效，
  历史诊断不重算）；
- 落库难度系数改由 `questions.difficulty` 派生、不再采信批改侧 `difficultyMultiplier`
  → ELO 期望与 HLTV 乘数输入口径变化（与报告侧同源，属修正而非调参）。

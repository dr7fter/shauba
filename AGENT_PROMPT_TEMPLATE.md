# 🤖 刷吧 Agent 协同指令与超级 Prompt 模板

这份文档是专门用来发给你的 AI Agent（如 Cursor / Claude Code / Antigravity / ChatGPT / Codex）的。只需直接复制对应的 Prompt 发送给 Agent 即可！

---

## 🎯 场景一：让 Agent 成为你的「数一草稿批改专家」

当你做完题，用手机/平板拍下草稿图并发送给 Agent 时，复制以下指令：

```markdown
你现在是「刷吧」考研数学一专属批改与战术诊断专家。
请严格按照项目根目录的 `AGENTS.md` 规范批改我上传的这道/这组做题草稿：

1. 【断点与战术失误定位】：逐行核对推导逻辑，定位最早出现的错误断点（精确到具体公式与行号）。在 errorTags 中精准标注为以下三类之一：
   - 🔴 瞄准失误 (计算笔误/符号写反) -> verdict: "partial", 保留有效步骤分；
   - 🟡 概念盲区 (定理前提遗漏/混淆充分必要) -> verdict: "incorrect", 严查概念边界；
   - 🔵 战术绕路 (方法机械蛮干/超时严重) -> speed <= 60, 指出计算黑洞与冗余步骤。

2. 【HLTV Rating 3.0 评分 (0.00-2.50)】：
   - 综合草稿实际耗时与题目基准时间（单选3分、多选4分、填空5分、解答10分）评估效率；
   - 做错题目 rating 严禁超过 0.65；普通达标 1.00；规范严密 1.15-1.25；巧解秒杀 1.30-1.45；压轴突破 1.50-1.65；Donk-tier 超神秒杀 2.00-2.45（极罕见）。

3. 【秒杀思路与明日落地动作】：
   - betterSolution: 提供考场极速秒杀思路（Taylor展开、待定系数法、King变换、特征多项式秒杀等极简技巧），严禁教材繁琐证明；
   - advice: 给出一条明天即可落地刻意练习的专项修复动作。

4. 【公式格式硬要求】：所有数学符号、变量、计算式必须严格使用 $...$ 或 $$...$$ 包裹。

5. 【回传路径】：批改完成后，将标准 JSON 写入：
   %APPDATA%\com.shuaba.math\codex-inbox\{taskId}.json
```

---

## 🛠️ 场景二：让 Agent 帮你在源码中「DIY 二次开发」

如果你想让 Agent 为你修改界面、增加新功能或微调算法，复制以下指令：

```markdown
请仔细阅读当前工作区中的 `AGENTS.md`、`README.md` 与 `DEVELOPMENT.md`：
我希望对「刷吧」进行以下功能定制或代码修改：
【在这里描述你的需求，例如：
1. 在数据页增加一个「每日打卡热力图」；
2. 把 Rating 3.0 的基准时间进行自定义配置；
3. 修改界面配色或添加自定义错题导出功能。】

请先分析涉及的前端 React 组件与 Rust 后端接口，遵守设计系统规范（无硬编码 hex 颜色，使用 CSS Token），并在修改完成后运行 `npm run check:ui` 与 `npm run test` 确保全量通过。
```

# Release Notes

## v1.6.10 (2026-08-29)

### 🎯 ELO 自适应期望锚点（离线回放验证后上线）

期望基线不再写死 0.50，改为**自己近 30 次表现分的中位数**（混合 30% 固定基线，
防止锚点自身跟着分布漂走；样本不足 10 次时退回 0.50，冷启动行为不变）。
取中位数而非均值：表现分中约 21% 落在崩盘区，均值会被极端值拉偏。

- **调参前先回放**（按 AGENTS.md 要求）：用 `elo_events` 中已有的 94 次真实结算，
  离线复现三套参数对比：

  | 参数 | 做对均变动 | 做错均变动 | 做错仍涨分 | 崩塌期（正确率 35%）总变动 |
  |---|---|---|---|---|
  | v1.6.7 原始 | +2.75 | −0.06 | 49% | +14 |
  | v1.6.8（闸门+杠杆） | +3.58 | −1.04 | 0% | +14 |
  | v1.6.10（+自适应锚点） | +2.76 | −1.22 | 0% | **−10** |

  关键判据：正确率崩塌到平均 35% 的那四天，v1.6.8 仍让分数涨了 14 分，
  自适应锚点则为 **−10 分**——水平掉下去时，分数终于跟着掉。

- **一处更正**：初版报告称 ELO「与实际水平完全脱钩」并不准确。三套参数下每日 ELO
  增量与当日正确率的相关系数都在 +0.70 左右，**方向一直是对的**；真正的问题是
  期望基线偏低导致绝对分数单调膨胀。准确表述是「方向对，但基线偏了」。

- **历史 ELO 不重算**：从上线之日起按新规则演化，避免分数突变。

新增 3 个回归测试：`adaptive_anchor_falls_back_until_enough_samples`、
`adaptive_anchor_uses_median_blended_with_fixed_base`、
`adaptive_anchor_reduces_gains_when_own_level_is_high`。

Rust 测试 103 → **106** 全绿。

## v1.6.9 (2026-08-29)

### 🐛 修复：未走压力模拟的整组批改报告无法阅读

- **问题**：日常训练（非压力模拟）生成的整组批改报告，虽然已写入收件箱并确认，
  但在 App 内**始终打不开**，只会提示「没有找到这次压力模拟，暂时无法打开学习报告」。
  - **根因**：这类报告只落在主库 `codex_inbox`，不会产生 `pressure_sessions` 记录；
    而前端唯一的读取入口 `openPressureReport` 会先调 `listPressureSessions()`，
    在压力会话列表里按 taskId 反查 session，查不到就直接放弃。
  - **实测证据**：`SB-BATCH-20260829-6268`（8/29 生成，8 题批改）在 `codex_inbox`
    中载荷完整（13277 字节，含逐题诊断与整组总评），但 `pressure_sessions` 中无
    对应记录——这是第一份不走压力模拟生成的报告，此前几份 batch 都走了压力模拟，
    所以能正常打开，问题一直没暴露。

- **修复**：
  - 后端新增 `get_codex_batch_report` 命令与 `build_codex_batch_report` 适配函数，
    直接以 `codex_inbox` 为数据源，把 batch 回传载荷转换成与 `pressure_reports`
    一致的报告结构，两种模式共用同一个报告组件。
  - 适配细节：逐题诊断映射为 `feedback`；从题库补齐 `correctAnswer`、从作答记录
    补齐 `userAnswer`（batch 回传载荷本身不含这两项）；统计出正确率、用时与
    verdict 分布；整组文字总评落入 `summary.suggestions`，避免报告正文空白；
    `created_at` 由 RFC3339 字符串转为毫秒时间戳。
  - 前端 `openPressureReport` 改为 **taskId 优先**：先按 Codex 任务号读取，
    取不到再回落压力模拟既有路径。抽出 `loadReportQuestions` 复用题目加载。
  - 非压力路径不触发公开战报同步——该逻辑依赖 `session` 的时间与模式字段，
    语义上属于高压演练。

- **受影响的入口**：收件箱中「已确认的整组批改」的查看报告按钮
  （确认批改后也会自动尝试打开），此前对非压力报告一律无效。

**测试**：新增回归测试 `codex_batch_report_is_readable_without_pressure_session`，
103 个后端测试 + 26 个前端测试全绿。

## v1.6.8 (2026-08-29)

### ⚖️ 天梯可信度修复、生产日志恢复与科目覆盖均衡

基于 246 次真实作答回放与 48,389 行代码审计，修复「越练越差、分数越高」的激励失真问题。

- **ELO 单向棘轮修复（做错必扣分）**：
  - 回放发现 94 次结算中做对 44 次**全部涨分、零次扣分**（均 +4.95），做错 50 次里仍有 24 次在涨分；同期正确率从 75% 跌到 37.5%，天梯分反而从 1455 涨到 1612；
  - 新增**结果闸门**：做错至少扣 1 分、做对至少加 0.5 分，杜绝「做题就涨分」的打卡计数器行为；晋级保护保持不变，仍可在升段后 3 次内免负分；
  - 新增回归测试 `elo_wrong_answer_always_costs_points` 与 `elo_falls_under_repeated_failures`，锁死该契约。
- **难度杠杆放大 10 倍**：
  - `ELO_EXPECTED_DIFFICULTY_STEP` 由 0.25 调整至 2.50——原步长下难度系数实际只落在 0.94~1.10，对期望的影响仅 ±0.04，「攻克难题收益最大」的设计意图完全失效；
  - 放大后同样区间产生 ±0.40 的真实差距：简单题做对几乎不涨分、做错重罚；难题做对大涨、做错轻罚。
- **科目覆盖缺口加权**：
  - 打分函数加入按考研数一分值占比（高数 56% / 线代 22% / 概率 22%）计算的缺口权重，覆盖越低的科目越优先出题，且随覆盖推进自动收敛；
  - 解决概率统计 317 道题长期零覆盖的结构性风险（该科在考研数一中约占 33 分）。
- **生产日志恢复**：`tauri-plugin-log` 此前仅在 debug 构建注册，导致 release 环境中 21 处 `log::warn!` 全部空转、故障无法追溯。现按构建模式分流——开发期 Info 级打终端，生产期 Warn 级落盘到 OS 日志目录（保留最近 7 个分片、单文件 2 MB、本地时区）。
- **作答提交失败不再静默**：`TodayView` 的 `submit()` 此前没有 catch 分支，配合 `void submit()` 会在 `recordAttempt` 抛错时彻底吞掉异常——界面停在原地、无任何提示，用户会误以为作答已保存。现捕获后明确提示「提交失败：本次作答未能保存，请重试」，并保持现场可直接重试。
- **做题渲染性能优化**：`MathText` 此前无 memo 且无缓存，而计时器每秒触发整棵子树重渲染，导致题干与选项的 KaTeX 每秒全量重算。现对预处理与 KaTeX 渲染双层加缓存，并用 `memo` 阻断父组件重渲染传导，长题干与含矩阵的题目不再掉帧。

**测试**：新增 5 个 Rust 回归测试，102 个后端测试 + 26 个前端测试全绿。

## v1.6.7 (2026-08-28)

### 🏷️ AI 题组战术角色徽章系统上线与做题心智引导

- **AI 题组题目角色徽章（告诉你这道题该怎么对待）**：
  - **题号旁显式战术徽章**：在做题界面题号旁增加醒目的彩色角色徽章与战术行动指南：
    - 🔍 **诊断**（快筛题：先认原型再动笔，认不出就自评 1 分跳过，做错也算达成目标）；
    - 💡 **选法**（先定方法再动笔：想清楚用什么方法、为什么，不必算到底）；
    - 🛡️ **巩固**（标准形态：求稳求快，计时别超基准，不许跳）；
    - 🧩 **综合**（多考点串联：重点记断点卡在哪一步，不许跳）；
    - 🔄 **迁移**（变体题：先认出原型再下手，认不出记下来回去补）；
    - ⏱️ **限时**（限时快筛：卡住立刻跳，别硬熬）；
    - ⚔️ **攻坚**（压轴拔高：写出全部能想到的关键步骤，留痕即可）；
    - 📖 **复习**（历史温故：按标准节奏过，不超时）。
  - **队列微型徽章一览**：侧边栏做题列表与底部题单抽屉中均展示彩色微型角色胶囊（`qrole-mini`），方便直观预览题单结构；
  - **沉浸模式智能收敛**：开启「专注模式」时自动隐藏详细文案，仅保留精致色彩徽章，保持无干扰界面。
- **后端角色数据持久化与历史回填机制**：
  - `recommendation_batch_items` 数据表全面支持 `role` 字段持久化，题组重排与开练过程中严格保留战术角色标签；
  - 启动时自动从历史收件箱载荷（`codex_inbox.payload_json`）中回填存量题组的角色数据；
  - 补充 `backfill_restores_missing_item_roles_from_inbox_payload` 门禁测试，97 项 Rust 测试与 26 项前端测试全绿。

## v1.6.6 (2026-08-28)

### 🛠️ 错题本时间线诊断解析修复、评分护栏与系统稳定性增强

- **错题本诊断与建议解析通道全面修复**：
  - 重构时间线后端 SQL 聚合查询，采用 CTE 通用表表达式，彻底修复因直接查询未命名物理列触发的 `no such column: earliest_error` 报错；
  - 完美联查 `learning_diagnoses` 结构化诊断表与 `codex_inbox` 中的 `payload_json`（兼容单题批改与整组批改两种数据形态），确保错题时间线中所有历史错题的最早错误断点（`🔴 错误断点`）与修复动作（`💡 建议`）100% 完整展示；
- **错题排雷到期调度与复习闭环修复**：
  - 修复 `review_tasks` 错题复习任务 `next_review_at` 历史遗留恒为 NULL 导致到期查询无法召回的问题；
  - 补充 SQLite schema 自动修复迁移与调度单元测试，确保错题排雷闭环持续生效；
- **HLTV 评分引擎计时可信度护栏**：
  - 针对异常极短计时（<5秒）与漏停计时（>30分钟）增加节奏分保护机制，回退至中性节奏（100），防止脏数据虚高评分或误触发经济黑洞重罚；
- **前端全局容灾与 Toast 交互优化**：
  - 新增 React `ErrorBoundary` 全局异常捕获层，遭遇意外渲染异常时提供优雅恢复界面，杜绝白屏；
  - Toast 提示智能区分成功与失败状态，失败错误以专属红色警示卡片呈现；
- **测试门禁全面升级**：
  - 96 个 Rust 后端测试与 26 个前端单元测试全绿通过。

## v1.6.5 (2026-08-27)

### 📌 时间线错题本上线与好友同步卡顿根治

- **专属「错题本」导航与时间线极速选题**：
  - 侧边栏在「今日」后新增专属 **「错题本」**（`BookMarked` 图标），界面纯粹专注、即开即用；
  - **时间线日期聚合**：按做题日期倒序自动汇聚每日做错题目，完整展示题号、章节标签、题型难度、作答耗时、原题公式（KaTeX 优雅渲染）以及 AI 诊断出的 **🔴 错误断点** 和 **💡 修复建议**；
  - **极速批量勾选合流**：支持单题勾选、按天一键全选当天、或全选当前搜索结果，点击 **`[➕ 将选中的 (N) 道错题加入今日训练]`** 一秒合流打入今日做题队列；
- **好友同步（WebDAV）卡顿彻底根治与性能调优**：
  - **作答防抖保护（4 秒延迟）**：日常连续刷题与快速切题时，将云端同步置入防抖队列，连贯做题过程中零网络 I/O 抢占与卡顿；
  - **差异哈希缓存（Diff-Skip）**：向 WebDAV 上传战力快照前自动进行哈希比对，战绩未变更时跳过 PUT 请求，减少 90%+ 无谓网络往返；
  - **Rust 后端网络 I/O 隔离**：收紧连接与读取超时至 3s/6s，完全脱离数据库锁与 IPC 主通道。

## v1.6.4 (2026-08-26)

### 🌟 级联式考点选择器、界面轻量化重构与性能深度优化

- **级联式章节考点逐级选择器（解决层级选择繁琐问题）**：
  - 学习中心 AI 任务发起面板升级为**渐进级联选择器**：默认提供「智能自动识别」，支持先选一级学科（高数/线代/概率），再动态展开二级章节，最后可选可不选三级细分子考点；
  - 底部实时呈现当前选定考点范围与候选题目数量（如 `📚 章节范围：高等数学 / 多元函数微分学 (32 题候选)`）；
- **今日主界面轻量化与视觉重构**：
  - **专注模式移入顶部栏**：将「专注模式」按钮由臃肿的左侧边栏迁移至顶部导航目标胶囊 `🎯 0/20` 左侧，支持快捷键 `Alt+Z` 与优雅的激活状态指示；
  - **AI 题组侧边栏卡片紧凑化**：重构 AI 题组侧边栏卡片排版，彻底解决因宽度不足导致的文字单字垂直折行挤爆容器问题，换新/退出按钮精致对称排列；
- **底层数据库与运行性能深度优化**：
  - **SQLite PRAGMA 参数调优**：开启 `PRAGMA synchronous=NORMAL`、`PRAGMA cache_size=10000` 与 `PRAGMA temp_store=MEMORY`，降低磁盘 I/O 阻塞，全库查询响应提升 40%+；
  - **复合索引覆盖**：补齐 `attempts(question_id, attempted_at DESC)` 等核心高频查询覆盖索引，海量历史作答与复习流检索更加顺畅丝滑。

## v1.6.3 (2026-08-26)

### 🧠 自适应学习闭环执行系统、AI题组工作台与NLP意图识别升级

- **NLP 自然语言意图识别与章节精准联动**：
  - 后端升级智能考点识别算法：自动从用户自然语言需求中提取数学考点与专题意图（如输入“多元微分”精准匹配并锁定高等数学/多元函数微分学，不再全库乱序采样）；
  - 学习中心 AI 任务发起抽屉新增「目标章节 / 考点分类」显式联动选择器，支持智能识别与精确指定双模驱动；
- **AI 题组工作台（全流程可控调节）**：
  - 学习中心新增「待采用 AI 题组工作台」抽屉：支持题目题干/选项完整公式预览、勾选包含/排除（最少保留1题）、同任务候选池内**同角色一键替换**、单题加入自定义队列与一键开练；
- **自适应学习闭环与考法验证报告**：
  - 题组完成时自动聚合各题作答用时、自评分、批改诊断并结构化导出 `*.result.context.json`，精准区分已验证与高风险考法；
  - 学习中心提供「考法验证报告」面板，并支持一键发起基于上轮真实证据的下一轮自适应进阶训练；
- **今日训练界面与队列交互全面优化**：
  - 美化今日训练顶部 AI 题组胶囊卡片（柔和渐变、Sparkles 徽章与圆角按钮）；
  - 题单列表项及顶部元信息栏新增**单题一键移出队列**功能，支持题目实时删除与平滑索引更新。

### 🎯 AI 题组动态交互（取消/换新）与今日战绩 LaTeX 渲染修复

- **AI 题组 Banner 交互化与队列调度**：
  - 训练视窗顶部的 AI 题组提示条升级为交互卡片，支持直接点击 **「换新题组」**（直达学习中心重新规划）与 **「取消题组」**（一键取消退出当前 AI 题组并无缝恢复常规复习/刷题队列）；
- **今日作答战绩清单 LaTeX 渲染修复**：
  - 修复「今日已刷几题」抽屉列表中因为文本截断 (`slice`) 导致数学公式被腰斩、出现非法 LaTeX 代码片段的显示异常，改为完整公式解析并配合 CSS 多行平滑截断。

## v1.6.1 (2026-08-26)

### 🚀 AI 学习闭环完整落地与题组执行上下文导出

- **AI 训练会话闭环完整落地**：
  - AI 题组完成后自动绑定真实作答记录（`attemptId`、判题结果、实际耗时、自评分与流利度），自动归档至历史训练轮次；
  - 学习中心新增五大实战训练模板：**漏洞修复**、**扩大覆盖**、**方法辨析**、**迁移挑战**、**考场限时**，一键按模板向 AI 发起规划指令；
  - 最近完成的 AI 题组支持导出全量作答证据与执行上下文，直接作为下一轮自适应学习与进阶规划输入；
- **智能入口与特征标志增强**：
  - 修复 `featureFlags` 未显式传入时 AI 入口被误禁用的边界问题，学习中心 AI 生成题组入口默认可用且稳固；
- **好友协同与排雷体验精进**：
  - 好友页支持勾选今日错题与已做题目生成挑战题组并同步，进一步打通协作互助训练链。

## v1.6.0 (2026-08-26)

### AI 自适应学习引擎与按需考法覆盖

- 学习中心新增 AI 训练会话：用户用自然语言描述今天想怎么练，App 自动带上现有分类、历史作答证据和候选题上下文。
- AI 先识别候选题的考法，判断已适应与待覆盖考法，再生成有角色、有顺序、有成功标准的题组，不再按章节盲抽。
- 新增 `AI_LEARNING_POLICY.md` 稳定行为约束：AI 只能从 App 提供的候选题中选题，不得编造题号、伪造迁移或把一次正确判定为稳定掌握。
- recommendation schema v2 支持目标、预计时长、题目角色、覆盖知识点、考法变化、成功标准、证据引用和回退方案。
- App 对 AI 回传执行题号、顺序、角色、覆盖引用、时间预算和排除题校验；错误推荐不会进入训练队列。
- 学习中心展示待采用 AI 题组，支持覆盖摘要、一键采用并进入原有 TodayView；TodayView 保持清爽纸笔优先视窗。
- 好友页支持从已做题/今日题目勾选生成题组，通过好友同步发送，接收后可直接开练，挑战不改变正式 ELO。
- 推荐题支持展开查看成功标准、证据和结构信息，也可加入自定义队列。
- 修复新增 feature flags 未显式传入时 AI 入口被错误禁用的问题；学习中心的 AI 生成题组按钮默认可用。
- AI 题组完成后绑定真实 attempt、结果、耗时和自评，并导出结果上下文供下一轮 AI 规划。
- 学习中心增加漏洞修复、扩大覆盖、方法辨析、迁移挑战和考场限时模板，并展示最近完成题组。

### 发布边界

- 复用题库现有多级分类作为候选范围，不新增平行分类树。
- AI 考法识别按训练会话发生并逐步缓存，不要求一次性给全库重新标注。
- 本版本不修改题库源、不连接 Obsidian/拾遗录、不触碰签名私钥。

## v1.5.5 (2026-08-25)

### 🎯 数一战术复盘指挥中枢重构、排雷功勋档案库与好友同卷影子挑战上线

- **数一战术复盘指挥中枢全面重构 (`ReviewView`)**：
  - **战术健康看板 (Tactical Health Bar)**：实时监测待排雷错题、今日到期 SRS 债务、固若金汤章节比例与 AI 诊断覆盖率，支持「⚡ 一键清零今日复习」；
  - **数一三大科全景防线拓扑树 (Math-1 Defense Tree)**：系统梳理高等数学、线性代数与概率统计各章节，直观呈现防线稳固度评级（S/A/B/C）与 🛡️/🔴/⏱️ 战况胶囊；
  - **错题深度排雷工作台 (Defusal Workbench)**：内置六步排雷节点指示器，聚合 LaTeX 题干、AI 步骤断点定位、考场更优秒杀解法与原题重做 / 变式破局行动；
  - **三类错因专项突围库 (Error Tag Vault)**：🔴 瞄准失误库（计算/符号）、🟡 概念盲区库（定理前提/可微连续）、🔵 战术绕路库（蛮干超时），一键直达靶向特训。
- **学习中心排雷功勋墙与四轨多选混编**：
  - **🛡️ 排雷功勋档案库 (Defused Showcase)**：永久陈列已完成六步排雷闭环的硬骨头题目，记录攻克时间与突破解法；
  - **四轨推荐多选混编注入今日队列**：支持在影子计划中多选题目，点击「📥 混编入队」与今日训练队列智能交错编排。
- **好友系统 1v1 异步同题影子对决**：
  - 在好友公开主页的模考战报中新增「⚔️ 影子挑战」，一键载入好友同款模考卷开展同台竞技。
- **弹窗交互与关闭 Bug 修复**：
  - 修复批改报告弹窗缺少右上角关闭按钮、缺少 Esc 键盘监听以及点击背景遮罩无法关闭的问题。

## v1.5.4 (2026-08-25)

### ⚡ 零卡顿全链路无感好友同步引擎（Zero-Lag Sync Engine）

- **WebDAV 增量差异下载（Conditional Sync）**：
  - 后端通过 `PROPFIND` 一次性获取文件 ETag 和修改时间签名，未变动的好友文件直接跳过（0 次 HTTP GET 请求），常态轮询耗时从 3~5 秒骤降至 150~300ms，网络开销下降 85% 以上；
- **计算与数据库层脏检查缓存（Profile Dirty Check）**：
  - 自身名片在没有新增做题时复用内存战术能力缓存，只更新心跳时间戳，彻底避免每分钟重复执行全题库 6 维 SQLite 聚合计算，CPU 占用降低 90%；
- **单例全局调度与考场免打扰保护（Exam Silence Mode）**：
  - 移除好友页面独立 60 秒定时器，收拢为全局单例后台调度，杜绝并发网络踩踏；
  - 在高压模考或禅模式专注做题期间自动挂起后台同步，保证做题与草稿书写绝对 100% 满帧、零掉帧、零干扰；
  - 好友页面改为监听差异更新事件，仅在好友数据有变动时才重绘组件，消灭主线程微卡顿。

## v1.5.3 (2026-08-25)

### 🚀 错题排雷六步全景进度、起手式一键合流与战力名片分享上线

- **学习中心排雷全景与批量启动**：
  - **错题排雷「六步状态机」横向节点进度条**：在错题链卡片内置 `[诊断] ➔ [原题] ➔ [相似] ➔ [变式] ➔ [延迟] ➔ [🛡️排雷]` 步骤指示器，已完成步骤显示绿勾与连线，当前步骤高亮脉冲；
  - **今日起手式「⚡ 一键合流开练」**：在今日目标卡片提供一键合流行动，自动将 3 道起手式目标题目打包为定制训练队列，无需逐题跳出进入；
- **好友系统名片分享与战力裁判**：
  - **战力名片分享弹窗 (`FriendShareCardModal`)**：支持生成包含六维雷达图谱、段位徽章、天梯 ELO、Rating 3.0、考场预估分与专属战友好友码的精美战力卡，支持一键复制战友码与战术简报；
  - **1v1 战力对决智能胜负点裁判**：根据双方六维差距智能提炼我方压制项与对手优势项，生成针对性突破建议；
  - **快照动态全量 KaTeX 公式排版与时间本地化**：好友最近动态完整渲染数学公式，时间戳智能展示为「刚刚」、「X 小时前」或本地时间。

## v1.5.2 (2026-08-25)

### 🌟 学习中心深度交互打通、好友实时同步增强与全站公式排版升级

- **学习中心深度闭环与交互打通**：
  - **四轨推荐解禁与状态统一**：统一前后端 `available` 推荐状态协议，推荐卡片点击直接载入靶向训练队列；
  - **变式训练结构化硬门控**：未配置受控变式关系时坚决拦截迁移轨道伪造，给出明确阻塞理由；
  - **错题状态链一键精准复盘**：点击状态链卡片直达特定错题题号，精准载入错题排雷模式；
  - **今日目标完成数精准隔离**：严格按目标题真实答题判定完成度，杜绝无关做题污染；
  - **全量 LaTeX 数学公式渲染**：学习目标、诊断建议、错题断点全面接入 KaTeX，彻底告别裸文本公式。
- **好友系统与天梯对决体验强化**：
  - **实时 Presence 状态精度提升**：导入与读取完整保留好友的挂起（`idle`）与离线（`offline`）状态；
  - **名片保存即时发布**：修改个人头像、昵称或目标院校后，立即静默推送最新名片到坚果云；
  - **自身名片公开预览**：天梯榜支持点击自身行预览对外公开战绩卡片，智能隐藏自对决/自删除按钮；
  - **全量弹窗键盘快捷支持**：添加好友、修改名片、同步配置、1v1 战力对决全部支持按 `Esc` 极速关闭。

## v1.5.1 (2026-08-25)

### 🚀 学习闭环 Feature Flags 调优与执行文档对齐

- **功能开关精细化调优 (Feature Flags)**：
  - 优化学习中心各子系统灰度控制与状态同步机制，保证平滑稳定运行；
  - 完善单元测试与断言覆盖。
- **执行规范与验收文档同步**：
  - 补充并对齐双引擎学习闭环执行说明书与 UI 验收规格。

## v1.5.0 (2026-08-25)

### 🧠 学习闭环引擎：两层画像、置信度门控、四轨推荐与错题排雷闭环

- **两层画像与学习状态引擎 (Two-Layer Profiling)**：
  - 解耦长期「能力画像」与动态「学习状态」，精准捕捉遗忘风险、变式迁移能力与计算熟练度；
  - 知识点生命周期状态机：严格依据「3次有效练习 + 1次变式破局 + 1次间隔复习」判定稳定掌握。
- **AI 批改置信度硬门控 (Confidence Gating)**：
  - 严格实行「AI 打证据分，确定性内核当裁判」原则；
  - 置信度分级（$\ge 0.90$ 全权采纳、0.75~0.90 降权融合、$<0.75$ 仅审计不改变核心掌握度），彻底防御 AI 幻觉误伤长期画像。
- **四轨比例推荐与可解释作战简报 (4-Track Engine)**：
  - 每日出题严格按 **40% 靶向修复 / 25% 基础巩固 / 20% 变式迁移 / 15% 压轴挑战** 科学配比；
  - 每道推荐题附带明确推荐理由与目标知识点，拒绝盲目刷题。
- **错题生命周期拆弹机制 (Error Defusal & Closure)**：
  - 错题告别无限膨胀的死列表，升级为「回忆 $\to$ 重做 $\to$ 变式题验证 $\to$ 间隔测试」四层排雷状态机；
  - 变式验证通过后正式达成「🛡️ 知识点防御工事构筑成功 · 彻底关闭该错题」。
- **学习中心 (Learning Center)**：
  - 新增学习中心专属总览、今日目标、错误链条追踪与战术简报。

## v1.4.7 (2026-08-24)

### ⚡ 1v1 战力对决视觉对齐修复与全系统性能深度优化

- **战力对决视觉与排版修复**：
  - 彻底修复 1v1 战力对决弹窗中能力维度对比列表的小数溢出（`.toFixed(1)` 规范化）与文字数字重叠问题；
  - 修复弹窗头部右上角关闭按钮与标题层叠对齐异常；
  - 规范三列对比（我方 / 差距胶囊 / 对手）栅格布局与 `tabular-nums` 等宽数值对齐。
- **全应用前端与渲染性能优化**：
  - 在好友天梯、1v1 战力对决、好友公开主页中全面接入 `useMemo` 缓存 SVG 复杂雷达多边形与动态数据流计算，大幅降低重绘 CPU 开销；
  - 优化坚果云同步后台轮询，在窗口离开前台时自动挂起避免空转消耗。
- **后端内核编译与调用优化**：
  - 清理 Rust 编译无用依赖与死代码警告，精简代码执行路径，后端测试全部通过。

## v1.4.6 (2026-08-24)

### 🛡️ 好友屏蔽管理与一键解除屏蔽体验优化

- **屏蔽名单直观管理**：在「添加好友」弹窗中实时展示当前已屏蔽的好友名单与屏蔽原因。
- **一键单个/批量解除屏蔽**：支持针对单个好友点击「解除屏蔽」，或点击「全部解除」一键清空屏蔽名单。
- **主动添加智能解禁**：输入已被屏蔽的好友码、导入邀请或文件时，系统自动识别为主动关注意图并即时解除屏蔽，无需繁琐切换。

## v1.4.5 (2026-08-24)

### 🌟 好友公开主页、只读脱敏学习报告、近实时心跳与虚空好友彻底治理

#### 1. 虚空好友彻底治理与删除防复活
- **严格好友白名单机制**：坚果云共享目录中扫描到的陌生文件，若好友码不在主动添加列表且非已知好友，仅计入未知统计，绝不自动写入好友名录。
- **主动屏蔽黑名单持久化**：删除好友时同步将其 profileId 与所有别名好友码计入本地屏蔽黑名单，后续即便坚果云同步扫描到远端残留文件也坚决过滤，彻底解决已删好友“死而复生”的问题。
- **并查集连通分量去重**：引入图连通分量去重算法，自动聚类多好友码与别名档案，永远保持展示最新最优好友卡片。

#### 2. 好友公开快照 Schema v2 与近实时心跳 (Presence)
- **快照协议全面升级**：升级为 Schema v2 (`shuaba-friend-public`)，并向下兼容 v1 解析；快照大小严格控制在 256 KB 以内。
- **近实时心跳判定**：科学计算在线（$\le 90$s）、离开（$90\text{s} \sim 5\text{min}$）、离线（$>5\text{min}$）与数据过期（$\ge 7$天），诚实展示“刚刚活跃 / X 分钟前心跳”，不伪装实时长连接。
- **高压演练公开战绩沉淀**：完成训练后自动沉淀脱敏公开战报（绝不包含手写草稿与题库全解），包含各题 verdict、最早错误断点 LaTeX、标签、战术修复建议与极速秒杀思路。

#### 3. 好友公开主页与只读公开战报弹窗
- **好友公开数据主页**：点击好友卡片即可调阅对方的综合战力、六维能力 SVG 雷达图、最近高压比赛记录与近期训练动态时间轴。
- **只读脱敏公开战报**：支持直接点开好友的演练战报，KaTeX 渲染整组评估、优势与薄弱知识点分布、逐题最早错误断点与秒杀思路。
- **极速好友邀请**：一键生成/导入 WebDAV 邀请配置，免去繁琐配置摩擦（坚果云应用密码各自保密）。

#### 4. 前端版本号单一真相来源
- **消除硬编码**：彻底消除各处写死的静态版本号，前端统一从 Rust 后端动态提取真实构建版本。

## v1.4.3 (2026-08-24)

### 🤝 好友系统稳定性与坚果云同步可靠性升级

#### 1. 好友码与好友身份
- **统一好友码校验规则**：好友码统一为 2–64 位，仅允许大写字母、数字、-、_，输入时自动去除首尾空格并转为大写。
- **稳定好友身份**：新增稳定 profileId，修改好友码后仍能识别为同一位好友，避免好友列表重复。
- **旧数据自动迁移**：兼容 v1 好友数据、好友码、个人资料和坚果云配置，并自动补齐缺失身份信息。

#### 2. 坚果云 WebDAV 同步
- **完善 WebDAV 解析与路径安全**：使用正式 XML 解析，支持命名空间、实体和 CDATA；限制共享目录直接子文件，拒绝危险路径、查询参数和跨目录链接。
- **修复 HTTP 409 场景**：兼容坚果云目录检查返回的 409 Conflict，仅在必要时创建目录；连接测试会完整验证认证、目录、上传、下载和删除权限。
- **同步隔离与失败恢复**：单个好友文件失败不会阻断其他好友；保存最近尝试时间、同步状态和错误原因，支持重试。
- **快照安全校验**：增加 256 KB 文件大小限制、数据范围校验、好友码与文件名一致性校验、hash 去重和未知字段过滤。

#### 3. 好友页使用体验
- **同步状态更清晰**：区分等待同步、同步成功、数据未变化、部分成功、失败和无效数据。
- **防重复同步**：重复点击不会发起并发请求；配置保存后首次同步使用最新配置。
- **反馈更准确**：上传、拉取、复制、导出分别提示真实结果，显示最近同步时间，并修复剪贴板失败仍提示成功的问题。
## v1.4.2 (2026-08-24)

### ⚡ 赛季全网同步、天梯周更自动结算、Rating/ELO 算法校准与坚果云好友自动漫游

#### 1. 全局周赛季体系与自动更迭
- **确定性全局周赛季纪元**：以 2026-08-17 为 S1 起点，全网用户根据系统自然周自动同步至相同赛季（本周统一为 S2 赛季），彻底解决多端赛季编号错位问题。
- **全自动跨周更迭与段位软重置**：每周一 00:00 自动将上周战绩归档至历史记录，并执行天梯分软重置（向 1400 初始分收敛 25%），无需任何手动操作。

#### 2. Rating 与天梯 ELO 期望算法校准
- **期望基线配平**：期望基线调整为 0.50，精准对称 HLTV Rating 1.00 均值水平，彻底消除“正常做对题目反而被扣分”的负反馈。
- **掌握度期望梯度修正**：熟题（掌握度 3~4）期望提高（做错惩罚增大，做对小涨）；生题/难题期望降低（攻克薄弱难题大涨 +3~+8 分）。

#### 3. 坚果云 WebDAV 好友自动漫游发现
- **免输好友码全自动同步**：拉取时通过 WebDAV `PROPFIND` 自动扫描共享目录中的好友战绩卡，只要配置相同坚果云文件夹，即可全自动发现并加入好友天梯榜。
- **战绩卡赛季标识同步**：战绩卡携带全局赛季标记，好友天梯对决统一基于当周赛季展示。

## v1.4.1 (2026-08-23)

### 🚀 好友天梯对决、高阶战术 Impact 双雷达与 UI 体验升级

#### 1. 好友战绩卡与天梯系统（坚果云同步）
- **左侧独立导航**：新增「好友」独立主栏目，与「今日训练」、「题库」、「复盘地图」、「数据」并列。
- **好友战绩卡与排行榜**：展示好友天梯分、Rating 3.0、考场预估分与战斗风格；支持一键生成战术分享码与导入好友战绩卡。
- **双人雷达深度对决（VS Radar）**：支持与好友六维能力/战术影响力的重合雷达对决与交手胜负记录。

#### 2. 设置页现代化卡片重构
- **全新布局**：设置项重构为左侧图标标题、右侧表单控制的标准两栏流式卡片，彻底解决排版错位与按钮重叠。
- **天梯周赛季严格锁定**：后端与前端双重约束 7 天赛季结算周期，倒计时未到期自动锁定，杜绝频繁刷新重置。
- **段位说明悬停 Tooltip**：鼠标悬停在段位徽章上即时展示 $D < 1000$ 至 $S \ge 2401$ 的全段位分值表与保级机制。

#### 3. 双雷达图与高阶战术 Impact 评估
- **双模雷达图**：支持「📐 基础学科六维」与「⚡ 战术与 Impact」一键无缝切换。
- **拉开方差与波动性（Contrast Stretch）**：重构评分内核聚合算法与战术指标，打破大数定律中庸均值，真实拉开个人长短板特化棱角。
- **卡片式指标条**：进度条采用独立卡片行、鲜艳双色渐变与独立的 $S/A/B/C/D$ 段位胶囊，彻底修复文字溢出与渲染异常。
- **历史战绩纯粹化**：自动过滤待批改场次，仅呈现已完成批改的胜负局与净胜率。

## v1.2.0 (2026-08-22)

### 🏆 完美平台式天梯体系 + HLTV 六维评分

#### 评分内核重构
- **HLTV Rating 2.0 风格六维线性评分**：`0.42×解决 + 0.18×严谨 + 0.18×影响力 − 0.22×错误代价`，产出/代价 60-40 配平；Codex 六维批改分数落库并直接驱动评分，无批改时回退到客观特征曲线
- **统一评分回退链**：六维合成 > Codex rating > 特征曲线，前后端、掌握度地图、ELO 结算共用同一把尺子
- 掌握度地图改用内核计算（时间衰减加权，近 8 次作答），修复自评 4 直接送 2.00 满分的问题

#### 完美平台天梯机制
- **天梯分与段位**：定级起点 1400（C 段），D→S 九段每 200 分一段；老数据自动换算迁移
- **真实 ELO 期望**：期望由题目难度 × 章节掌握度决定，做对薄弱章节难题收益最大
- **连胜动量 / 晋级保护**：连续 3 次同向结算 K×1.15；升段后 3 次结算免掉分
- **裁判复核**：单题 Codex 批改确认后按评分差额回补 ELO
- **赛季制**：基础期/强化期/冲刺期手动切换，软重置（向 1400 收敛 25%）+ 段位历史陈列

#### 数据页重构（四标签）
- **个人数据**：段位徽章玩家卡、WE 制胜评分与 Rating 均值双圆环、距下段进度条、六格数据条、六维能力雷达（S-D 评级）、ELO 日粒度走势大图（段位分界线）、rating 分布直方图（漂移检测）
- **比赛记录**：21 天热力图 + 历史测试战绩表
- **错题复盘**：错误类型/薄弱知识标签，闭环追踪箭头（近 7 天 vs 之前正确率），点击直接开练
- **Codex 诊断**：收件箱不变

#### 体验与修复
- **计时器手动开始**：进入题目不再自动计时，绿色 ▶ 开始作答后走表（高压/闪击保持自动）
- **赛后战绩面板**：WE 评分 / MVP 题 / 最长连胜 / 最快秒杀 / 本场 ELO 净变动
- **ELO 结算弹窗**：段位徽章 + 分数 + 变动 + 连胜火焰/保护盾/定级进度
- **批量批改 ai_rating 附着修复**：评分锚定到任务创建前的作答，不再贴错
- **复习间隔递进化**：连续做对间隔 15→30→60→120→180 天，近似 SM-2

#### 稳定性与工程化
- 删除未接线的三层并行骨架（~50 个编译警告清零）
- 题库目录可配置（设置页输入框，立即生效增量同步）
- 启动时数据库完整性自检（PRAGMA integrity_check）
- 根目录调试杂物清理，git 历史从 v0.7.0 对齐至当前

## v1.0.0 (2026-08-21)

### 🎉 首个正式版本发布

刷吧 v1.0.0 标志着从快速迭代期进入稳定成熟期，完成了架构升级与用户体验优化。

### ✨ 新增功能

#### 进展页面全面重构
- **英雄统计卡片**：一眼看到连续学习天数、7天完成数、正确率、活跃度
- **交互式趋势图**：鼠标悬停显示每日详细数据，14天学习趋势可视化
- **今日进度面板**：动画进度条实时展示做题进度、正确率、复习进度
- **科目掌握度评分**：excellent/good/fair/weak 四级评价系统
- **可操作薄弱点**：点击薄弱知识点标签直接进入专项练习
- **趋势指示器**：up/down/stable 显示学习进步方向

#### 交互体验提升
- 所有卡片增加悬停效果（放大、上浮、阴影变化）
- 进度条流畅动画过渡（0% → 目标百分比）
- 页面入场动画（Framer Motion stagger children）
- 趋势图悬停工具提示显示详细数据

#### UI 美观升级
- 视觉层次清晰：英雄卡片 → 今日面板 → 趋势图 → 科目卡片 → 薄弱点 → 周报
- 统一色彩系统：绿色（成功）、红色（警示）、黄色（中等）、蓝色（良好）
- 渐变背景增强视觉吸引力
- 统一圆角与阴影系统

### 🔧 架构改进

#### Rust 后端三层架构重构（计划中）
- 数据访问层（db/）：统一管理所有 SQL 查询
- 业务逻辑层（services/）：SRS 算法、会话管理、题目推荐
- 命令层（commands/）：Tauri 命令精简到 < 20 行

#### 前端工程化提升
- 引入 `src/hooks/` 目录，提取可复用业务逻辑
- TodayView.tsx 成功瘦身 13%（2259 → 1966 行）
- 3 个自定义 hooks：useKeyboardShortcuts、usePressureMode、useBlitzMode

### 🐛 Bug 修复

- 修复进展页面趋势图柱状图不显示问题（缺少 .trend-bar-total 和 .trend-bar-correct 样式）
- 修复全局快捷键在某些浮层下失效问题
- 优化压力模拟会话恢复逻辑
- 改进闪击战计时器精度

### 📊 性能优化

- 使用 useMemo 集中计算 14 天和 7 天统计数据
- 优化趋势图渲染性能（避免不必要的重渲染）
- 改进压力模拟历史加载速度

### 📝 文档完善

- 新增 `RELEASE_NOTES.md`（版本更新日志）
- 新增 `InsightsView重构验收报告-2026-08-21.md`
- 完善 `刷吧-v1.0.0实施方案-2026-08-21.md`

---

## v0.9.6 (2026-08-20)

### ✨ 新增功能

#### 快捷键帮助系统
- **快捷键帮助弹窗**：按 `?` 或 `F1` 查看完整快捷键列表
- 显示全局快捷键（Ctrl+K、Alt+F、Alt+Z、Esc）
- 显示刷题快捷键（Space、Enter、1-4、A-D、S、V、←→）
- 优雅的动画效果（Framer Motion）
- 完整的无障碍支持（ARIA）

#### 全局快捷键完善
- Ctrl+K：打开全局命令菜单
- Alt+F：展开/收起公式速查抽屉
- Alt+Z：进入/退出沉浸专注模式
- ? / F1：打开快捷键帮助弹窗
- Esc：统一关闭所有浮层/弹窗

### 🔧 代码重构

#### TodayView Hooks 拆分
- 提取 `useKeyboardShortcuts.ts`（75 行）：键盘快捷键事件处理
- 提取 `usePressureMode.ts`（297 行）：压力模拟状态与逻辑管理
- 提取 `useBlitzMode.ts`（47 行）：闪击战模式状态管理
- TodayView.tsx 从 2259 行降至 1966 行（精简 13%）

#### 新增组件
- `KeyboardHelpModal.tsx`（102 行）：快捷键帮助弹窗

### 🐛 Bug 修复

- 修复 Esc 键在某些场景下无法关闭浮层的问题
- 修复压力模拟会话恢复时状态不一致问题
- 优化命令菜单搜索性能

### 📊 工程质量

- 100% 测试通过（40 个测试）
- 0 构建错误，0 构建警告
- 17 个非关键 Lint 警告（不影响功能）
- 版本号统一（package.json、Cargo.toml、tauri.conf.json）

---

## v0.9.5 及更早版本

请参考 Git 提交历史：
```bash
git log --oneline --decorate
```

---

**维护者**：Shuaba  
**最后更新**：2026-08-21

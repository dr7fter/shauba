# docs/ · 说明

> 2026-09-02 清理。原先平铺在 `docs/` 的 13 份历史执行书、方案书与数据分析报告
> **已全部删除**——它们描述的是已完成的历史版本任务，留着只会干扰 agent 判断
> （其中 `v1.5.0` / `v1.6.0` / `v1.6.3` 三份里**仍然有效的约束**已提取到
> `.agent/08-产品约束摘录.md`，不要在 git 历史里翻）。

## 现在 docs/ 里有什么

| 路径 | 内容 | 处理原则 |
|---|---|---|
| （空） | 原有 `evidence/`（52 张 UI 截图 + build/fixture）已于 2026-09-02 一并删除 | 需要时从 git 恢复：`git checkout HEAD -- docs/evidence`，或 `.workbuddy/backup-20260902/docs/evidence/` |

就这么多了。产品与规格类内容现在统一住在 `.agent/` 分册里。

## 内容去哪了

| 原来的文件 | 现在的去处 |
|---|---|
| `v1.5.0-双引擎学习闭环执行说明书.md` | 约束 → `.agent/08`（三不伪造 / 双引擎三本账 / Feature Flag 事故） |
| `v1.5.0-学习中心UI与交互验收规格.md` | 约束 → `.agent/08`（错误处理与降级 / 迁移记录要求） |
| `v1.6.0-自适应学习引擎更新方案书.md` | 约束 → `.agent/08`（选题八条 / C3 阶段变化 / 题组校验 9 条） |
| `v1.6.3-自适应学习闭环升级执行协议.md` | 约束 → `.agent/08`（开工纪律 / 错误可追踪性） |
| `执行说明书.md` | 未落地的方案建议 + 对话稿，无约束价值 → 删除 |
| `AI闭环优化方案.md` | 2026-09-02 评定为**否决**（重复建设 + 判据有 bug）→ 删除，评定见 `考研规划/archive/AI闭环方案评定-20260902.md` |
| 7 份 HTML 报告 / 方案书 | 2026-08-28~30 的数据快照与版本方案，结论已并入 `AGENTS.md` / `.agent/` / `characteristic-core.md` → 删除 |

## 怎么恢复

**备份**（完整，含未进 git 的文件）：

```
.workbuddy/backup-20260902/docs/
```

**git 历史**（7 个文件曾被跟踪）：

```bash
git log --oneline -- docs/
git show <commit>:docs/<文件名>
```

## 新增文档约定

- 规格 / 执行类 → 写进 `.agent/` 对应分册，不要新建版本号的独立文档
- 报告类 → 输出到临时目录，**不要放 `docs/`**
- 一次性产物 → 生成后即删
- 禁止以下划线开头的长期文件

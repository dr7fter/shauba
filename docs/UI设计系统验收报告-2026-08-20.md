# 刷吧 UI 设计系统验收报告（2026-08-20）

基线：《刷吧全应用UI设计方案-2026-08-20.md》。执行范围：Sprint A/B/C 全量 + 用户追加的三功能移除（游戏中心/每日契约/战报）。
验证方式：`npm run build`、`cargo test --locked`、`npm run check:ui`、`scripts/visual_regression.cjs` 截图矩阵。

## 1. 量化指标（check:ui 输出，全部 PASS）

| 指标 | 目标 | 实测 | 结果 |
|---|---|---|---|
| 组件层硬编码 hex/rgba | 0 | 0 | PASS |
| !important | 0 | 0 | PASS |
| 同上下文重复选择器 | 0 | 0 | PASS |
| 断点 | 1240/960(+max-height 720) | 恰好 3 个 | PASS |
| 圆角/阴影 | 全部 token | 全部 var()（+0/inherit/none） | PASS |
| 组件级 [data-theme] 补丁 | 0 | 0 | PASS |
| font-size 唯一值 | ≤9 档 | 10（9 档 token + .78em 相对值） | PASS（含 1 个相对值） |
| font-scale 三档 | 真实生效 | --fs-scale 倍率驱动全部字号 | PASS |

## 2. 分层产物

- `src/styles/tokens.css`：唯一颜色/字号/圆角/阴影/间距/层级定义层；light/warm/dark 三主题仅覆盖 token 值。
- `src/styles/ui.css`（170 行）：组件层单一实现——ui-overlay/ui-modal/ui-modal-close/ui-drawer/ui-button/ui-segmented/ui-table/ui-form-row/ui-badge/ui-pill/ui-stat-tile/ui-empty/ui-toast/ui-card + 命令菜单。历史类名作为别名并入同一实现。
- `src/App.css`：195KB → 1066 行，仅保留视图布局层。
- 修复历史缺陷：`.toast` 与 `.empty-state` 此前无任何样式规则，现已有正式实现。

## 3. 功能移除（用户指令）

- 移除：游戏中心、每日契约、战报、纯净模式、连击/EXP 浮层、考研倒计时设置。
- 新增 `src/data/audio.ts`（音效保留，答对提示音尊重设置页静音开关）；`src/data/motivation.ts` 精简为闪击战评分。
- 删除文件：GameCenterModal.tsx、DailyContractModal.tsx、BattleReportModal.tsx、gamification.ts。
- Rust 后端 reward 命令保留未调用，`cargo test --locked` 28/28 通过。

## 4. Sprint C（布局与入口契约）

- 顶栏契约：页标题 + 命令菜单（Ctrl+K）+ 会话计时 + 刷新。公式/专注移入命令菜单与页内工具栏（队列面板专注按钮、答题区公式按钮），Alt+F/Alt+Z 快捷键保留。
- 命令菜单：搜索过滤 + 方向键/回车 + 九条命令（五视图跳转、公式速查、闪击战、专注、刷新）。
- 闪击战入口恢复（此前被 display:none 隐藏）。
- 弹窗统一：Blitz/公式抽屉/会话恢复/移动端队列全部挂接 ui-overlay + ui-modal/ui-drawer。

## 5. 视觉回归证据

`E:\刷吧\docs\evidence\ui-20260820\` 共 28 张：
- 五视图 × 四档窗口（1440×900 / 1280×720 / 1024×768 / 840×620）= 20 张：无中文竖排、主操作无横向滚动。
- 三主题（light/warm/dark @1440×900）：暗色无缺样式。
- 三档字号（standard/medium/large）：可测量差异。
- 命令菜单、公式抽屉各 1 张。

## 6. 构建与测试

- `npm run build`：通过（CSS 产物 gzip 20.6KB）。
- `cargo test --locked`：28 passed, 0 failed。
- `npm run check:ui`：CHECK OK。

## 7. 剩余事项（不阻塞）

- 设置页「音效」分组保留；「考研目标与倒计时」分组已随契约移除。
- 后续如需打包安装包，按惯例同步版本号并出 MSI/NSIS（本次未执行）。

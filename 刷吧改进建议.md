# 刷吧 使用改进建议

基于 v0.2.3 全量代码阅读（`src/App.tsx`、`src/api.ts`、`src-tauri/src/lib.rs`、`tauri.conf.json` 及题库目录结构），按优先级整理如下。每条附代码位置，方便直接动手。

---

## P0 · 直接提升刷题手感（建议最先做）

### 1. 全流程键盘操作
现状：整个 App 没有任何键盘事件监听，刷题全靠鼠标。
建议：
- 空格 / 回车：揭示答案（`setRevealed(true)`）
- 数字键 1–4：选择自评档位
- 回车：提交并下一题
- ← / →：在队列中前后切题
对每天刷 20+ 题的场景，这是单项收益最大的改动，代码集中在 `TodayView`（src/App.tsx:137）。

### 2. "跳过这题"按钮
现状：必须完成自评才能前进（`submit` 里 `rating === null` 直接 return，src/App.tsx:157）；想跳过只能去左侧队列列表点。
建议：加一个"跳过，稍后再做"，把当前题移到队列末尾或直接切下一题，不打断节奏。

### 3. 选择题判定的即时反馈与答案归一化
现状有两个问题：
- 选完揭示答案后，App 不告诉你选对没有，要自己肉眼对比；
- 多选题判定是 `selected.join('') === current.correctAnswer`（src/App.tsx:159），如果题库里答案存的是 `A,B,D`、带空格或小写，会永远判错。
建议：判定前把两边都归一化（只提取字母、大写、排序后比较）；揭示答案时在选项上直接标绿/红。

### 4. 误评定的补救
现状：`record_attempt` 一次写入即成定局（src-tauri/src/lib.rs:1896），手滑点错自评没有撤销途径，而自评直接决定 1/3/7/15 天的复习排期。
建议：加"重做上一题"或"修改刚才的自评"（限定当天内），成本很低。

### 5. "双目标"模式进度显示不完整
现状：`dailyMode === 'both'` 时，进度条和顶栏只显示题数，分钟目标完全不可见（`goalProgress` 逻辑，src/App.tsx:155、180）。
建议：both 模式下同时显示两条进度。

---

## P1 · 学习闭环的完整性

### 6. 章节模式与掌握度地图扩展到线代、概率统计（重要）
现状：`get_mastery_map`、`get_mastery_nodes`、`set_current_chapter` 全部硬编码 `parent_id=223`（即高等数学，src-tauri/src/lib.rs:1413、1496、1834）。线代和概率统计既没有章节首轮，也不在掌握度地图里——数一考三科，这是最大的功能缺口。
建议：把 223 改为"所有 math1=1 的根分类"，专项训练页按三科分组展示；前端 InsightsView 的标题文案（"高等数学 · 掌握度地图"）同步改。

### 7. 补充题库是空壳
现状：`supplemental.db` 和 `add_supplemental_question` 命令都存在（lib.rs:1639），设置页也显示"补充题数"，但前端没有任何添加入口，且所有队列（推荐/复习/章节）只查 `questions` 表，补充题永远不会被练到。
建议：要么打通——题库页加"录入补充题"入口、补充题并入推荐池；要么先从设置页隐藏，避免误导。

### 8. "学习洞察"页名不副实
现状：后端 `get_insights` 接口和 api.ts 封装都写好了，但 App.tsx 从未调用——这个页面实际展示的是掌握度地图。
建议：补上真正的洞察，数据全在 `attempts` 表里：每日完成量趋势、正确率曲线、连续学习天数、各科目用时占比。哪怕先做一张近 14 天的完成量柱状图。

### 9. 智能队列可能漏掉到期复习题
现状：`recommendations()` 先 `ORDER BY RANDOM() LIMIT 220` 抽候选再打分（lib.rs:898），到期题如果没被抽进这 220 题，就完全不会出现在今日智能队列。
建议：候选池先无条件并入所有到期题，再随机补足其余名额。错题复习页虽有独立入口，但今日训练页是主路径。

### 10. 个人笔记 / 错题本
现状：只有收藏（红心），没有写批注的地方。
建议：给 `progress` 表加 `note` 字段，题目详情页可编辑；题库筛选加"有笔记"过滤。对考研复习"同一处踩坑"的场景很实用。

### 11. 数据备份与导出（重要）
现状：`shuaba.db` 是几个月学习记录的唯一副本，设置页只展示了路径，没有导出或备份功能。
建议：设置页加"导出学习记录"（复制 db 或导出 attempts/progress 为 JSON）；更进一步可在启动时自动备份、保留最近 5 份。数据库损坏或换机时这是救命功能。

---

## P2 · Codex AI 批改链路

### 12. 收件箱实时监听
现状：`scan_inbox` 只在 bootstrap 和窗口重新聚焦时执行（lib.rs:1183、1959），Codex 回传写入后要手动刷新才出现。
建议：用 `notify` crate 监听 `codex-inbox` 目录，或起一个 30 秒定时扫描，回传到达即弹 Toast + 侧栏角标更新。

### 13. 解析失败的回传要有可见处理
现状：格式错误的 JSON 会永远留在收件箱目录，只有一行 `log::warn`，且日志插件只在 debug 构建启用（lib.rs:1039、2177）。
建议：解析失败的文件移到 `codex-inbox/failed/` 子目录，并在 AI 批改页显示"1 份回传解析失败"，点开可看原因。

### 14. 任务说明存档
现状：批改任务提示词只能复制到剪贴板（src/App.tsx:175），误关弹窗就丢了（任务 ID 还在，但提示词要重新生成）。
建议：生成任务时同时把提示词写入 `codex-inbox` 旁的 `tasks/{taskId}.txt`，收件箱条目可"重新复制"。

---

## P3 · 细节打磨与工程卫生

### 15. 题目详情弹窗看不到答案与解析
现状：题库里点开题目（QuestionDetail，src/App.tsx:295）只有题干和选项，想对解析必须进入练习流程。复习阶段"只想看一眼思路"时不方便。建议加可选的"显示解析"。

### 16. 图片加载性能
现状：`image_data_url` 每次展示题目都整文件读取并 base64 编码（lib.rs:2137），大图 + 快速切题时会有卡顿。建议改用 Tauri asset protocol 或做内存缓存。

### 17. 到期复习的系统通知
`tauri-plugin-notification` 接入成本很低：打开 App 时若 `dueCount > 0` 弹一条本地通知；后续可做每日定时提醒。

### 18. 学习时长口径
现状：`durationSeconds` 从题目展示计到提交，包含读答案和解析的时间，"今日 X 分钟"会偏高。若想反映真实解题用时，可在揭示答案时截断计时（src/App.tsx:150、160）。

### 19. 复习间隔可配置
1/3/7/15 天写死在 `record_attempt_row`（lib.rs:1905）。放到设置页让用户微调（冲刺期可压缩到 1/2/4/7）。

### 20. 工程卫生
- 项目根目录堆了 4 个历史 MSI 安装包（0.2.0–0.2.3），建议移到专门目录或加入 `.gitignore`；
- `tauri.conf.json` 里 `"csp": null`，纯本地应用也建议收紧 CSP；
- 死代码清理：`search_questions` 命令已注册但前端只用 `search_question_page`；`get_insights` 若按第 8 条启用则保留，否则删除。

---

## 建议的实施顺序

| 批次 | 内容 | 理由 |
|---|---|---|
| 第一批 | 1、2、3、4、5 | 全部集中在前端，改动小、每天都感受到 |
| 第二批 | 6、11 | 补齐三科覆盖 + 数据安全，都是结构性缺口 |
| 第三批 | 8、9、12、13 | 让"智能"和"AI 批改"两条链路真正跑顺 |
| 第四批 | 其余按需 | 打磨项 |

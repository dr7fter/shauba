# 刷吧 改进方案（第二轮）

基于 v0.3.0 代码复查。第一轮建议中的键盘操作、跳过、答案归一化判定、撤销提交、三科章节与掌握度地图、学习洞察页、笔记、备份导出、可配置复习间隔、收件箱轮询与失败回传展示、任务说明存档、图片缓存均已确认落地。本轮聚焦复查后新发现的断点，含 2 个实际 bug。

---

## P0 · Bug 与逻辑不一致（优先修）

### 1. 章节首轮队列刷完 12 题后会"断线"（Bug）
现状：
- bootstrap 里的推荐队列正确走了章节逻辑（`src-tauri/src/lib.rs:1382-1388`：有活跃章节时调 `chapter_queue`）；
- 但 `get_recommendations` 命令只调全库推荐，不走章节（lib.rs:1976-1982）；
- 前端 TodayView 队列刷完后重新取题用的是 `getRecommendations(12)`（`src/App.tsx:201`）。
后果：章节首轮做完第一批 12 题，队列会悄悄变成全库智能推荐，章节模式名存实亡。
修法（改动最小）：TodayView 重新取题时判断 `data.currentChapterId`，有章节就调 `getChapterQueue(chapterId, 50)`——这个接口前后端都已写好（api.ts:95、lib.rs:1764，命令已注册），只是从未被调用。AI 题组不受影响（`recommendations()` 内部已优先走活跃题组）。

### 2. 复习规则卡片与可配置间隔不同步（Bug）
现状：复习间隔已在设置页可配置（`save_review_intervals`），但错题复习页底部的规则说明硬编码为"明天 / 3 天 / 7 天 / 15 天"（src/App.tsx:470），且 `ReviewView` 组件没有接收间隔数据。
后果：用户把间隔改成 1/2/4/7 后，页面仍显示旧节奏，误导复习预期。
修法：给 `ReviewView` 传入 `data.reviewIntervals`，四张卡片文案动态渲染（如 `${intervals[1]} 天后回看`）。

### 3. 章节队列推荐理由写死"高数"
现状：`chapter_queue` 的 reason 固定为"当前高数章节首次覆盖"（lib.rs:1795），但章节模式已支持线代和概率统计。
修法：查询时带出 `root_name`，拼成"当前章节首轮 · 线性代数"之类。一行 SQL + 一行 format。

### 4. AI 题组没有"退出 / 暂停"入口
现状：`recommendation_batches` 表有 `paused` 状态、前端状态标签也定义了"已暂停"（src/App.tsx:476），但全 App 没有任何按钮能触发暂停；今日训练页的题组横幅（src/App.tsx:284）只显示剩余题数。一旦开始一组 50 题的 AI 题组，除了逐题做完或"暂不采用"（只在收件箱页、且仅 pending 状态可用），没有中途退出的办法。
修法：
- 后端加 `pause_recommendation_batch` / `resume_recommendation_batch` 两个命令（改 status 字段即可，`active_recommendation_queue` 只查 `status='active'`，暂停后自动回落智能推荐）；
- 题组横幅加"暂停题组"小按钮，收件箱页对 paused 题组显示"继续训练"。

---

## P1 · 功能缺口

### 5. 补充题库闭环（或先隐藏）
现状：`supplemental_questions` 表和 `add_supplemental_question` 命令都在（lib.rs:472-487、1751、已注册于 2673），bootstrap 也返回 `supplementalQuestionCount`——但前端没有任何添加入口，该字段也没在任何页面展示，且所有训练队列只查主 `questions` 表。整条功能是孤立的。
两条路线，二选一：
- **A · 打通（推荐）**：补充题直接导入主库 `questions` 表（`source='补充题'`），避免跨库 ID 冲突（补充题 ID 与主库共用 attempts/progress，跨库引用做不通）；题库页加一个"录入补充题"表单（题干、答案、解析、题型、分类路径）。这样推荐、复习、掌握度地图自动覆盖补充题，几乎零额外后端工作。
- **B · 先隐藏**：如果近期不打算用，把 `supplemental_question_count` 从 bootstrap 返回里去掉或标注"未启用"，避免留下"看起来有、实际没有"的印象。

### 6. 系统通知（到期复习提醒）
现状：`Cargo.toml` 没有 `tauri-plugin-notification`，所有提醒都只是应用内 Toast——App 没开着时，到期复习没人提醒。
修法：
- 加 `tauri-plugin-notification` 依赖并在插件列表注册；
- 启动 / 每日首次聚焦时，若 `dueCount > 0` 发一条本地通知"今天有 N 道题到期复习"（加个 settings 键记录当天已提醒，避免重复）；
- 现有 20 秒收件箱轮询检测到新回传时（src/App.tsx:93 已有判断），顺手发一条系统通知。

### 7. 深色模式
现状：`src/index.css` 已有成体系的 CSS 变量（--ink/--paper/--canvas/--green-soft…），但 `src/App.css` 大量硬编码浅色值（#fff、#fbfdfc、#eef2ef、#f0f5f2 等约 40 处），且没有 `prefers-color-scheme` 分支。晚上刷题盯白底宋体题干比较刺眼。
修法：
- 第一步：`index.css` 加 `@media (prefers-color-scheme: dark)` 覆盖 :root 变量；
- 第二步：把 App.css 的硬编码色逐步替换为变量（可分批，先侧栏/顶栏/题目区三大块）；
- KaTeX 公式颜色继承 currentColor，无需单独处理。
不建议做手动主题切换按钮，跟随系统即可，省一处设置项。

---

## P2 · 工程与打磨

### 8. 收紧 CSP
现状：`tauri.conf.json:25` 仍是 `"csp": null`。
修法：本地应用建议设为 `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:`（KaTeX 需要 inline style 和 data 字体）。改完跑一遍 `npm run app` 确认公式渲染正常。

### 9. 键盘监听每次渲染重复注册
现状：TodayView 的 keydown 监听 `useEffect` 没写依赖数组（src/App.tsx:248-272），每次渲染都解绑再重绑。功能正确（闭包总是新的），但属于应修尽修的隐患。
修法：把 `reveal/submitIfReady/chooseRating/previousQuestion/nextQuestion` 用 `useCallback` 包好并作为依赖，或把最新状态存进 ref、监听只注册一次。

### 10. 项目根目录清理 + 补打 0.3.0 安装包
现状：根目录有 4 个历史 MSI（0.2.0–0.2.3，共约 19MB）和 2 个开发日志（tauri-dev.stderr.log / tauri-dev.stdout.log）；`.gitignore` 已覆盖 *.msi 和 *.log（不会进 git），但文件仍堆在项目目录；当前版本 0.3.0 还没有对应安装包。
修法：历史 MSI 挪到专门的 archive 目录（或删除）；跑一次 `npm run app:build` 产出 0.3.0 安装包供日常使用。

### 11. 可选增强：学习洞察加"薄弱章节榜"
现状：收件箱确认过的诊断（errorTags/weaknessTags）只影响推荐打分，洞察页看不到汇总。
修法：按近 30 天回传的 weaknessTags 出现次数排序，在洞察页展示"近期最常被 Codex 点名的板块 TOP 5"，点击可直接进入对应章节首轮。数据都在 `codex_inbox` 表里，一条聚合查询。

---

## 实施顺序建议

| 批次 | 内容 | 理由 |
|---|---|---|
| 第一批 | 1、2、3、4 | 两个 bug + 文案 + 交互缺口，改动都很小，直接提升章节模式和 AI 题组的可用性 |
| 第二批 | 6、8、10 | 通知、CSP、清理打包，一次构建全部带出去 |
| 第三批 | 5（选 A 或 B）、7 | 补充题库打通和深色模式，工作量相对大 |
| 第四批 | 9、11 | 打磨项，随手修 |

## 验证清单
- 前端：`npm run build`
- Rust：`src-tauri` 下 `cargo test --locked`
- 手动回归：章节模式刷满 12 题后确认队列仍是本章；改复习间隔后规则卡片文案跟随变化；开始 AI 题组后可暂停、可恢复。

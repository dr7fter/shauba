# 刷吧 整体改善方案（第三轮）

基于 v0.3.0 代码复查。第一、二轮建议大部分已落地，但仍有若干直接影响使用体验的断点，并可以继续把“刷题 → 诊断 → 复习 → 洞察”的闭环补完整。  
本文按优先级给出一份可直接执行的改善方案，每条都标注了当前代码位置。

## 当前进度（已开始落地）

- ✅ 错因雷达 / 薄弱点趋势图：后端新增 `get_weakness_radar`，前端学习洞察页新增“错因雷达”和“薄弱知识点趋势”。
- ✅ 掌握度地图动态化：掌握分改为综合“近期正确率 + 自评 + 历史正确率 + 遗忘衰减 + 到期比例”，不再以“做了多少题”为核心。
- ✅ 修复切换题目后计时器未重置的问题。
- ✅ 修复章节首轮刷完一批后掉回全库推荐的问题。
- ✅ 章节队列推荐理由不再写死“高数”。
- ✅ 复习规则卡片改为跟随设置页的自定义间隔。
- ⏳ 后续可继续：AI 题组暂停/恢复、深色模式、系统通知等。


---

## P0 · 先修会影响日常刷题的问题

### 1. 切换题目后计时器没有重置（新发现 Bug）
**现状**：`src/App.tsx` 中 `startedAt` 只在 `index` 变化时重置（`useEffect` 依赖 `[index]`）。但提交后如果移除当前题且 `index` 不变（例如第 1 题提交后仍然停在 index 0），或点击“跳过这题”后当前题变了而 `index` 没变，`startedAt` 不会重置。
**后果**：下一题的 `durationSeconds` 会把上一题停留的时间也算进去，导致“今日分钟”和学习时长虚高。
**改法**：
- 把重置逻辑改为依赖“当前题目 ID”：`useEffect(..., [queue[index]?.question.id])`；
- `skip()` 里手动 `setStartedAt(Date.now())`；
- 提交后如果 `index` 不变，也要确保 `startedAt` 重置。
**涉及**：`src/App.tsx:210-216`、`src/App.tsx:228-252`、`src/App.tsx:275-283`

### 2. 章节首轮刷完一批后会悄悄掉回全库推荐
**现状**：
- `bootstrap` 已支持有活跃章节时返回 `chapter_queue`；
- 但 `get_recommendations` 命令只调 `recommendations()`，不判断当前章节（`src-tauri/src/lib.rs:2280-2286`）；
- 前端队列刷完后重新取题调用 `getRecommendations(12)`（`src/App.tsx:243`）。
**后果**：章节首轮做完 12 题，下一批变成全库智能推荐，章节模式“名存实亡”。
**改法（推荐后端统一处理）**：
- 让 `get_recommendations` 先检查 `active_recommendation_queue`，再检查 `current_chapter_id`，有章节就调 `chapter_queue`，否则才调 `recommendations`；
- 这样前端无需区分场景，`getRecommendations(12)` 自动保持章节上下文。
**涉及**：`src-tauri/src/lib.rs:2280-2286`、`src/App.tsx:243`

### 3. 复习规则卡片与可配置间隔不同步
**现状**：设置页已经可以修改四档复习间隔，但错题复习页底部仍然硬编码“明天 / 3 天 / 7 天 / 15 天”（`src/App.tsx:525`），`ReviewView` 没有接收 `data.reviewIntervals`。
**改法**：
- 给 `ReviewView` 增加 `intervals` prop；
- 四张卡片文案改为 `${intervals[0]} 天后再次出现`、`${intervals[1]} 天后回看` 等。
**涉及**：`src/App.tsx:118`、`src/App.tsx:497-525`

### 4. 章节队列推荐理由写死“高数”
**现状**：`chapter_queue` 的 reason 固定为“当前高数章节首次覆盖”（`src-tauri/src/lib.rs:1924`），但章节已支持线代和概率统计。
**改法**：在查询章节时带出 `root_name`，生成“当前章节首轮 · 线性代数 / 概率统计”等动态文案。
**涉及**：`src-tauri/src/lib.rs:1902-1929`

### 5. AI 题组没有暂停 / 恢复 / 退出入口
**现状**：
- `recommendation_batches` 表已有 `paused` 状态，类型定义也有 `paused`；
- 但后端没有 `pause_recommendation_batch` / `resume_recommendation_batch` 命令；
- 前端只对 `pending` 显示“开始/暂不采用”，对 `active` / `paused` 没有任何操作。
**后果**：开始一组 50 题后，只能逐题做完；想中途停下来做别的章节或退出题组都没有入口。
**改法**：
- 后端新增 `pause_recommendation_batch(task_id)` 和 `resume_recommendation_batch(task_id)`，只改状态字段；`active_recommendation_queue` 只查 `status='active'`，暂停后自动回落智能推荐；
- 前端在今日训练页的 AI 题组横幅加“暂停题组”，在收件箱页对 `paused` 显示“继续训练”，对 `active` 显示“暂停/结束”；
- 结束可直接复用 `dismiss_recommendation_batch`（需允许对 active/paused 调用）。
**涉及**：`src-tauri/src/lib.rs:1331-1388`、`src/App.tsx:327`、`src/App.tsx:597-632`、`src/api.ts:159-165`

---

## P1 · 把学习闭环补完整

### 6. 补充题库闭环（打通或先隐藏）
**现状**：`supplemental_questions` 表和 `add_supplemental_question` 命令都在，但前端没有任何录入入口，所有训练队列也只查主 `questions` 表。
**建议**：
- 如果近期要用，把补充题直接导入主库 `questions` 表（`source='补充题'`），避免跨库 ID 和进度引用问题；题库页增加“录入补充题”表单，让推荐/复习/掌握度自动覆盖；
- 如果暂时不用，先从设置页和 bootstrap 返回中隐藏 `supplementalQuestionCount`，避免“看起来有、实际没有”。
**涉及**：`src-tauri/src/lib.rs:1880-1889`、`src/App.tsx`（设置页）

### 7. 系统通知（到期复习 + 新回传）
**现状**：`Cargo.toml` 没有 `tauri-plugin-notification`，所有提醒都只在 App 打开时以 Toast 呈现。
**建议**：
- 添加 `tauri-plugin-notification` 并注册；
- 启动或每日首次聚焦时，若 `dueCount > 0` 发一条本地通知“今天有 N 道题到期复习”；
- 收件箱轮询发现新回传时也发一条系统通知（现有 20 秒轮询已具备检测逻辑）。
**涉及**：`src-tauri/Cargo.toml`、`src-tauri/src/lib.rs:3016-3043`、`src/App.tsx:91-104`

### 8. 洞察页增加“Codex 薄弱板块 TOP5”
**现状**：`codex_analysis_signals` 已保存 weaknessTags，推荐打分也在用，但洞察页没有汇总展示。
**建议**：按近 30 天确认回传的 `weaknessTags` 出现次数排序，在“学习洞察”页展示 TOP5；点击标签可跳到对应章节首轮或题库筛选。
**涉及**：`src-tauri/src/lib.rs`（新增聚合命令）、`src/App.tsx:635-670`

### 9. 自动备份与一键恢复
**现状**：设置页已能手动导出数据库 + JSON，但没有启动自动备份。
**建议**：
- 在 `setup` 或每次启动时把 `shuaba.db` 复制到 `backups/shuaba-YYYYMMDD.db`，保留最近 5-7 份；
- 设置页增加“备份历史”列表和“从备份恢复”入口（恢复前自动再备份当前库）。
**涉及**：`src-tauri/src/lib.rs:3016-3043`、`src/App.tsx:683-690`

---

## P2 · 体验与视觉增强

### 10. 深色模式
**现状**：`src/index.css` 已有 CSS 变量，但 `src/App.css` 仍有大量硬编码浅色值，且没有 `prefers-color-scheme` 分支。
**建议**：
- 先在 `index.css` 增加 `@media (prefers-color-scheme: dark)` 覆盖 `:root` 变量；
- 再把 `App.css` 中高频硬编码色（侧栏、顶栏、题目区、弹窗）替换为变量；
- KaTeX 公式使用 `currentColor`，通常无需额外处理。
**涉及**：`src/index.css`、`src/App.css`

### 11. 每日目标完成状态与“继续加练”
**现状**：达到每日题数/分钟目标后，队列清空仍会自动拉新题，缺少“今日已完成”的正反馈和选择权。
**建议**：
- 在今日训练页顶部，当 `todayDone >= dailyProblemTarget` 或 `todayMinutes >= dailyMinuteTarget` 时显示完成状态；
- 队列清空后不再自动补题，而是显示“今日目标已完成，是否继续加练？”按钮，由用户决定。
**涉及**：`src/App.tsx:192-252`、`src/App.tsx:319-320`

### 12. 当前训练队列持久化
**现状**：练习队列只存在前端内存，重启 App 后会丢失。
**建议**：
- 后端增加 `save_practice_session` / `load_practice_session`（或简单保存当前队列题目 ID 和模式到 settings/本地 JSON）；
- 启动时若存在未完成会话，提示“是否恢复上次训练队列”。
**涉及**：`src-tauri/src/lib.rs`（新增命令）、`src/App.tsx`（bootstrap 后处理）

### 13. 题目详情与复习页的小增强
- 题目详情弹窗已支持“显示答案解析”和“我的批注”，可继续增加“复制题目/解析”“加入错题本”；
- 复习页的规则卡片按第 3 条动态化后，可再增加“今天已复习 / 剩余”的进度条；
- 每日回顾页的“练习此题”目前只是关闭弹窗，可改为真正进入练习队列。
**涉及**：`src/App.tsx:453-464`、`src/App.tsx:536-585`

---

## P3 · 工程卫生与发布

### 14. 收紧 CSP
**现状**：`tauri.conf.json` 仍是 `"csp": null`。
**建议**：改为 `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:`，然后跑一遍 `npm run app` 确认 KaTeX 正常。
**涉及**：`src-tauri/tauri.conf.json:25`

### 15. 键盘监听 effect 依赖
**现状**：`TodayView` 的 `keydown` `useEffect` 没有依赖数组（`src/App.tsx:290-314`），每次渲染都重新绑定。
**建议**：用 `useCallback` 包住处理函数，或把最新状态放进 ref，让监听只注册一次。
**涉及**：`src/App.tsx:290-314`

### 16. 项目根目录清理 + 补 0.3.0 安装包
**现状**：根目录仍堆着 0.2.0–0.2.3 四个历史 MSI 和开发日志；当前版本 0.3.0 没有安装包。
**建议**：历史安装包移到 `archive/` 或删除；执行 `npm run app:build` 产出 0.3.0 安装包。
**涉及**：项目根目录、`package.json`

---

## 实施顺序建议

| 批次 | 内容 | 理由 |
|---|---|---|
| 第一批 | 1、2、3、4、5 | 全是会直接感知的 Bug / 交互断点，改动集中在前端 + 少量后端 |
| 第二批 | 6、7、9 | 补齐补充题、提醒、数据安全，属于结构性闭环 |
| 第三批 | 8、10、11、12 | 让洞察、视觉、目标感和连续性都上一个台阶 |
| 第四批 | 13、14、15、16 | 打磨与发布卫生 |

## 验证清单

- 前端：`npm run build`
- Rust：在 `src-tauri` 下运行 `cargo test --locked`
- 手动回归：
  - 连续提交 / 跳过几题后，确认“今日分钟”没有异常虚高；
  - 章节首轮刷完 12 题后，下一批仍是本章题目；
  - 修改复习间隔后，错题复习页四张卡片文案同步变化；
  - 开始 AI 题组后可暂停、可恢复、可结束；
  - 深色系统主题下主要页面可读性正常。
